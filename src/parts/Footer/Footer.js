import React from "react";
import {FooterText, StyledFooter} from "./Footer.styles";

const Footer = () => {
    return (<StyledFooter>
        <FooterText>RKS</FooterText>
    </StyledFooter>);
};

export default Footer;
